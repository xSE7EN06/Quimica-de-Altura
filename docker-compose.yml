services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4200:80"
    container_name: quimica-de-altura-app
    # Optional: override API URL at runtime (requires env substitution in build or runtime config)
    # environment:
    #   - GATEWAY_URL=http://host.docker.internal:8000

  # Uncomment and configure if you run the auth/API backend in Docker:
  # api:
  #   image: your-api-image:latest
  #   ports:
  #     - "8000:8000"
  #   container_name: quimica-de-altura-api
